<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yami.shop.dao.CouponUserMapper">

    <select id="countByUserAndCoupon" resultType="int">
        SELECT COUNT(*) FROM tz_coupon_user
        WHERE user_id = #{userId} AND coupon_id = #{couponId}
    </select>

    <update id="useCoupon">
        UPDATE tz_coupon_user
        SET status = 1,
            use_time = #{now},
            order_number = #{orderNumber}
        WHERE coupon_user_id = #{couponUserId}
          AND user_id = #{userId}
          AND status = 0
          AND user_start_time &lt;= #{now}
          AND user_end_time &gt;= #{now}
    </update>

    <update id="restoreByOrderNumber">
        UPDATE tz_coupon_user
        SET status = CASE WHEN user_end_time &lt; #{now} THEN 2 ELSE 0 END,
            use_time = NULL,
            order_number = NULL
        WHERE order_number = #{orderNumber}
          AND status = 1
    </update>
</mapper>
